## 💡 Краткое содержание subject'а:

- C++ - Модуль 07: Этот модуль содержит 3 упражнения и  модуль разработан, чтобы помочь вам понять шаблоны в CPP:
- **Упражнение 00**: A few functions:
 ```
  Напишите следующие шаблоны функций:
  - swap: Меняет местами значения двух аргументов. Ничего не возвращает.
  - min: Сравнивает два аргумента и возвращает наименьший из них. Если два аргумента
  равны, то возвращается второй.
  - max: Сравнивает два аргумента и возвращает
```
- **Упражнение 01**: Iter:
 ```
  Напишите шаблон функции iter, которая принимает 3 параметра и ничего не возвращает.
  Первый параметр - адрес массива, второй - длина массива и третий - функция,
  вызываемая для каждого элемента массива.
  ```
 - **Упражнение 02**: Array
```
	Напишите шаблон класса Array, который содержит элементы типа T и который позволяет
	следующее поведение:
  - Конструкция без параметра: создает пустой массив.
  - Конструкция с unsigned int n в качестве параметра: создает массив из n элементов,
  инициализированный по умолчанию. (Совет: попробуйте скомпилировать 
  int * a = new int();, а затем выведите на экран * a.)
  - Конструирование с помощью оператора копирования и оператора присваивания.
  В обоих случаях модификация одного из двух массивов после копирования/присвоения не повлияет ни на что в другом массиве.
  - Вы ДОЛЖНЫ использовать оператор new[] для присваивания. Вы не должны делать
  превентивное распределение. Ваш код никогда не должен обращаться к нераспределенной
  памяти.
  - Доступ к элементам осуществляется с помощью оператора operator[].
  - При доступе к элементу с помощью оператора operator[], если этот элемент находится
  вне пределов, возникает исключение std::exception.
  - Функция-член size, которая возвращает количество элементов в массиве. Эта 
  функция-член не принимает никаких параметров и не изменяет текущий
  экземпляр каким-либо образом.
 ```

